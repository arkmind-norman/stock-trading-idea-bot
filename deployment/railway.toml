# This file is superseded by railway.json at the project root.
# Railway reads railway.json (or railway.toml) from the root of the repo.
# The Dockerfile referenced here was never created; nixpacks is used instead.
